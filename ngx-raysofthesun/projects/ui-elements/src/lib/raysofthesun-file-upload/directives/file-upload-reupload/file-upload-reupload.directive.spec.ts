import { FileUploadReuploadDirective } from './file-upload-reupload.directive';

describe('FileUploadReuploadForDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadReuploadDirective();
    expect(directive).toBeTruthy();
  });
});
