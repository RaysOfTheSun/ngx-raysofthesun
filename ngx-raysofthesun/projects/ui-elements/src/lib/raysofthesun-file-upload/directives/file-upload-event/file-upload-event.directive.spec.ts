import { FileUploadEventDirective } from './file-upload-event.directive';

describe('FileUploadEventDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadEventDirective();
    expect(directive).toBeTruthy();
  });
});
