import { FileUploadDeleteDirective } from './file-upload-delete.directive';

describe('FileUploadDeleteDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadDeleteDirective();
    expect(directive).toBeTruthy();
  });
});
