import { FileUploadInputDirective } from './file-upload-input.directive';

describe('FileUploadInputDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadInputDirective();
    expect(directive).toBeTruthy();
  });
});
