import { FileUploadPreviewDirective } from './file-upload-preview.directive';

describe('FileUploadPreviewDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadPreviewDirective();
    expect(directive).toBeTruthy();
  });
});
