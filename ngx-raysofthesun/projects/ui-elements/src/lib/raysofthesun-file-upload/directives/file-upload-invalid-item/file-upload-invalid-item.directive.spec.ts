import { FileUploadInvalidItemDirective } from './file-upload-invalid-item.directive';

describe('FileUploadInvalidItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadInvalidItemDirective();
    expect(directive).toBeTruthy();
  });
});
