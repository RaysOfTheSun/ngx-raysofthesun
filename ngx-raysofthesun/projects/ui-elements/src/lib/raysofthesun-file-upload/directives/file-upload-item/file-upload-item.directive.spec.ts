import { FileUploadItemDirective } from './file-upload-item.directive';

describe('FileUploadItemDirective', () => {
  it('should create an instance', () => {
    const directive = new FileUploadItemDirective();
    expect(directive).toBeTruthy();
  });
});
